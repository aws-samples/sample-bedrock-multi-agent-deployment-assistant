Parse this Fortinet and AWS cloud deployment document into structured text for vector search.

Rules:

1. PRESERVE NUMERICAL DATA: throughput (Gbps), instance types (c5.xlarge), port numbers, CIDR ranges, costs ($), license tiers (FG-VM01), vCPU/RAM specs.

2. FORTIGATE CLI: Keep full config/set/next/end syntax. Prefix with "[CLI: purpose]" (e.g., "[CLI: HA active-passive setup]").

3. TABLES: Convert to "Header: Value" pairs per row.

4. ARCHITECTURE PATTERNS: Extract on labeled lines:
   - Pattern: (name)
   - Use Case: (what it solves)
   - AWS Services: (list)
   - FortiGate Config: (HA mode, interfaces, routing)
   - Traffic Flow: (ingress to egress path)

5. HA CONFIGURATIONS: Label mode (active-passive/active-active/FGCP/FGSP), failover mechanism, AWS-specific behavior (ENI migration, EIP reassignment, route table updates).

6. SECURITY FEATURES: Tag with category (IPS, antivirus, web-filter, app-control, SSL-inspection, DLP) and compliance frameworks (PCI-DSS, HIPAA, SOC2).

7. DIAGRAMS: Describe as "ComponentA -> ComponentB (protocol/purpose)".

8. Extract everything. Do not summarize, omit, or add commentary.
