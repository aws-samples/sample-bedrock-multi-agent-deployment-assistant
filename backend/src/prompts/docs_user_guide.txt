Write a User Guide for a FortiGate-VM deployment on AWS.
Output ONLY the Markdown content — no preamble, no wrapping fences, no meta-commentary.
Use tables, bullet points, and numbered lists for clarity.
TARGET LENGTH: approximately 3000 words. Be concise but thorough — prioritize
actionable information over verbose explanations.

The guide MUST include the following sections in order:

## 1. Project Overview
- What this deployment achieves and business context
- Architecture summary: all AWS services used, how they interconnect, data flow,
  network topology
- Design rationale: why this deployment pattern was chosen, trade-offs,
  HA strategy, security posture
- Prerequisites: AWS account requirements, CLI tools, licenses, IAM permissions

## 2. CloudFormation Template Walkthrough
For each resource in the CloudFormation template:
- Explain what it does and why it exists
- List key properties and their significance
- Group resources by category: Networking, Security, Compute, IAM, Monitoring, etc.
Use a table format where appropriate:
| Resource | Type | Purpose | Key Properties |
Also explain the Parameters section and Outputs section.

## 3. Deployment Guide
- Step-by-step deployment instructions with CLI commands
  (aws cloudformation deploy, parameter overrides, stack monitoring)
- Post-deployment verification: health checks, connectivity tests
- FortiGate licensing activation
- HA synchronization setup (if applicable)

## 4. Production Configuration
- FortiAnalyzer integration
- CloudWatch monitoring and alarms
- Backup procedures
- Troubleshooting table: Symptom | Cause | Resolution

=== CONTEXT ===

Approved Design:
{design_json}

Requirements:
{requirements_json}

CloudFormation Template:
{cft_template}